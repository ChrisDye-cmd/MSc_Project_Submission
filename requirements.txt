# Core dependencies for the analysis pipeline.
# Versions pinned to those used during the dissertation (Python 3.x).

pandas==2.3.0
numpy==2.3.0
matplotlib==3.10.5
seaborn==0.13.2
statsmodels==0.14.5
scipy==1.16.0
scikit-learn==1.7.0
hmmlearn==0.3.3

# Jupyter (to run the notebooks)
Jupyter >= 5.4
